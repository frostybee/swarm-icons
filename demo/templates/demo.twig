<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    .icons { display: flex; gap: 1.5rem; flex-wrap: wrap; align-items: center; }
    .icon-card { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; }
    .icon-card span { font-size: 0.75rem; color: #666; }
    svg { width: 32px; height: 32px; }
  </style>
</head>
<body>
  <h1>{{ title }}</h1>

  <div class="icons">
    {% for item in icons %}
      <div class="icon-card">
        {{ icon(item.name, {'aria-label': item.label, 'role': 'img'}) }}
        <span>{{ item.name }}</span>
      </div>
    {% endfor %}
  </div>
</body>
</html>
